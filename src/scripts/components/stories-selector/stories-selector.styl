.storiesSelector
  position: relative
  width: 100%
  height: 100%
  background-color: #10161A
  pointer-events: auto

.header
  display: flex
  justify-content: space-between
  padding: 10px
  background-color: #182026

.title
  flex-grow: 1
  padding-right: 75px
  padding-right: 75px
  color: #fff
  text-align: center
  text-transform: uppercase
  font-size: 1.1em

.backButton
  padding: 10px
  color: #fff
  font-size: 1.1em
