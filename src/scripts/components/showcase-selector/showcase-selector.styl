@require '../../../variables.styl'

.showcaseSelector
  position: relative
  display: flex
  flex-direction: column
  width: 100%
  height: 100%
  background-color: $black
  pointer-events: auto

.disabledButton
  color: $textDefault
  font-family: NotesEsa
  pointer-events: none

  svg
    fill: $textDefault

  &:hover
    color: $textDefault
