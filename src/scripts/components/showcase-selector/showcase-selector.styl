.showcaseSelector
  position: relative
  width: 100%
  height: 100%
  background-color: #fff
  pointer-events: auto

.header
  display: flex
  justify-content: space-between
  align-items: center
  padding: 10px
  background-color: #182026

.title
  color: #fff
  text-align: center
  text-transform: uppercase
  font-size: 1.1em

.backButton
  color: #fff
  font-size: 1.1em

.playButton
  display: flex
  align-self: flex-end
  align-items: center
  padding-right: 20px
  color: #939597

  svg
    padding-left: 10px
    fill: #fff
