@require '../../../variables.styl'

.storyFooter
  display: flex
  align-items: center
  height: emCalc(72px)
  height: $footerHeight
  background-color: $darkGrey1

.fadeOutFooter
  opacity: 0
  transition: opacity 0.4s ease-in-out

  &:hover
    opacity: 1
