.story
  width: 50%
  height: 100%
  pointer-events: none

.sidepanel
  position: relative
  background-color: #fff
  pointer-events: auto

.previewImage
  width: 100%
  height: 25%
  background-color: #ccc
  object-fit: cover

.content
  padding: 10px

.backButton
  position: absolute
  left: 0
  display: block
  padding: 20px 0 0 20px
  color: #fff
  pointer-events: auto
