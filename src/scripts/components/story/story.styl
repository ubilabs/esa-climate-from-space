.story
  position: relative
  display: flex
  flex-direction: row
  justify-content: flex-end
  width: 100%
  height: 100%
  pointer-events: none

.sidepanel
  width: 30%
  background-color: #fff
  pointer-events: auto

.previewImage
  height: 25%
  background-color: #ccc

.content
  padding: 10px
